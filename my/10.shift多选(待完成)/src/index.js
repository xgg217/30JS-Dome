(function() {
  "use strict";

  const inboxDom = document.querySelector(".inbox");

  document.addEventListener("keydown", function(e) {
    if((e.target.nodeName !== "INPUT") && (e.key !== "Shift") ) { return; }
    
  })


})();